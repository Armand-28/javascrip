let estacionamiento = 1500;
let horas = prompt("Ingrese las horas de estacionamiento");
let minutos = prompt("Ingrese los minutos");
let = estacionamiento = horas * estacionamiento
horas++;

if (estacionamiento = horas && estacionamiento = minutos);{
    document.write("El costo de estacionamento es :",estacionamiento)
}

